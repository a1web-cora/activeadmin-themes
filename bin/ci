#!/usr/bin/env bash
# bin/ci

source "$(dirname "$0")/_lib.sh"
"$BIN_DIR/test-mise"
run_bundle rspec
run_bundle rubocop
run_bundle rbs validate
run_bundle bundle-audit check --update
run_bundle rake build
